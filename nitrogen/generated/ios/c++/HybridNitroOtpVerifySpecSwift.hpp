///
/// HybridNitroOtpVerifySpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridNitroOtpVerifySpec.hpp"

// Forward declaration of `HybridNitroOtpVerifySpec_cxx` to properly resolve imports.
namespace NitroOtpVerify { class HybridNitroOtpVerifySpec_cxx; }



#include <string>
#include <vector>
#include <NitroModules/Promise.hpp>
#include <functional>

#include "NitroOtpVerify-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::nitrootpverify {

  /**
   * The C++ part of HybridNitroOtpVerifySpec_cxx.swift.
   *
   * HybridNitroOtpVerifySpecSwift (C++) accesses HybridNitroOtpVerifySpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridNitroOtpVerifySpec_cxx can directly inherit from the C++ class HybridNitroOtpVerifySpec
   * to simplify the whole structure and memory management.
   */
  class HybridNitroOtpVerifySpecSwift: public virtual HybridNitroOtpVerifySpec {
  public:
    // Constructor from a Swift instance
    explicit HybridNitroOtpVerifySpecSwift(const NitroOtpVerify::HybridNitroOtpVerifySpec_cxx& swiftPart):
      HybridObject(HybridNitroOtpVerifySpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroOtpVerify::HybridNitroOtpVerifySpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridNitroOtpVerifySpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline std::shared_ptr<Promise<std::vector<std::string>>> getHash() override {
      auto __result = _swiftPart.getHash();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::string>> requestHint() override {
      auto __result = _swiftPart.requestHint();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> startOtpListener(const std::function<void(const std::string& /* otp */)>& handler) override {
      auto __result = _swiftPart.startOtpListener(handler);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> stopOtpListener() override {
      auto __result = _swiftPart.stopOtpListener();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    NitroOtpVerify::HybridNitroOtpVerifySpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::nitrootpverify
