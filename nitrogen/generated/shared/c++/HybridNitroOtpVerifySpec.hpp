///
/// HybridNitroOtpVerifySpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <vector>
#include <NitroModules/Promise.hpp>
#include <functional>

namespace margelo::nitro::nitrootpverify {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroOtpVerify`
   * Inherit this class to create instances of `HybridNitroOtpVerifySpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroOtpVerify: public HybridNitroOtpVerifySpec {
   * public:
   *   HybridNitroOtpVerify(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroOtpVerifySpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroOtpVerifySpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroOtpVerifySpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<std::string>>> getHash() = 0;
      virtual std::shared_ptr<Promise<std::string>> requestHint() = 0;
      virtual std::shared_ptr<Promise<void>> startOtpListener(const std::function<void(const std::string& /* otp */)>& handler) = 0;
      virtual std::shared_ptr<Promise<void>> stopOtpListener() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroOtpVerify";
  };

} // namespace margelo::nitro::nitrootpverify
